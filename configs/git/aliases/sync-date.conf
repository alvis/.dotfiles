[alias]
	# Rebase unpushed commits so their committer dates match their author dates.
	# Usage: git sync-date
	sync-date = "!git rebase --committer-date-is-author-date HEAD~$(git rev-list --count @{u}..HEAD)"
