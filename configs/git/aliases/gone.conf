[alias]
	# Run the installed gone executable found through PATH.
	# Usage: git gone
	gone = "!f() { origin=$(git config --show-origin --get alias.gone | cut -f1); conf=${origin#file:}; exec \"$(dirname \"$conf\")/gone\" \"$@\"; }; f"
